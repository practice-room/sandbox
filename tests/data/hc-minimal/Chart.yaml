# This Chart.yaml is expected to be regenerated on-demand during E2E testing.
apiVersion: v2
name: "hc-minimal" # Changes for every execution
description: A Helm chart for Kubernetes
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: '>= 1.14.0-0'
annotations:
  charts.openshift.io/name: "hc-minimal"
